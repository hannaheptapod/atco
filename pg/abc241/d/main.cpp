#include <bits/stdc++.h>
using namespace std;

int main() {
    int Q;
    cin >> Q;
    vector<int> a;
    int leng = 0;

    for (int i = 0; i < Q; i++) {
        int c, x, k;
        cin >> c;

        switch (c) {
            case 1:
                cin >> x;
                break;

            default:
                break;
        }
    }

    return 0;
}